# .github/workflows/build.yml
# Ultimate Scaler CI 构建流水线（含 Minecraft 源码反编译，CFR 版）
name: build

on:
  push:
    branches: [ "main", "dev" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

env:
  JAVA_VERSION: 21
  JAVA_DISTRIBUTION: temurin

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
      - name: 拉取代码
        uses: actions/checkout@v4

      - name: 校验 Gradle Wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: 配置 JDK
        uses: actions/setup-java@v4
        with:
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          java-version: ${{ env.JAVA_VERSION }}

      - name: 设置 Gradle 缓存
        uses: gradle/actions/setup-gradle@v4
        with:
          cache-read-only: ${{ github.ref != 'refs/heads/main' }}

      - name: 给 gradlew 加执行权限
        run: chmod +x ./gradlew

      - name: 生成 Minecraft 源码（下载字节码包）
        run: ./gradlew genSources

      # ============================================================
      # 用 CFR 反编译器提取 Java 源码
      # ============================================================
      - name: 反编译 Minecraft 源码（CFR）
        id: decompile
        run: |
          set -e
          MC_VERSION=$(grep '^minecraft_version=' gradle.properties | cut -d= -f2)
          echo "📌 Minecraft 版本: $MC_VERSION"

          mkdir -p build/mc_src build/all_jars

          # 1. 定位 merged-unpicked.jar
          SOURCE_JAR=$(find ~/.gradle/caches -type f -name "merged-unpicked.jar" | head -1)
          if [ -z "$SOURCE_JAR" ] || [ ! -f "$SOURCE_JAR" ]; then
            echo "❌ 未找到 merged-unpicked.jar，跳过反编译"
            exit 0
          fi
          echo "✅ 找到字节码包: $SOURCE_JAR"
          cp "$SOURCE_JAR" build/all_jars/merged-unpicked.jar

          # 2. 下载 CFR 反编译器
          CFR_JAR="build/cfr.jar"
          if [ ! -f "$CFR_JAR" ]; then
            echo "📥 正在下载 CFR 反编译器..."
            wget -q -O "$CFR_JAR" "https://repo1.maven.org/maven2/org/benf/cfr/0.152/cfr-0.152.jar"
          fi

          # 3. 执行反编译
          echo "🔧 开始反编译 merged-unpicked.jar 到 build/mc_src ..."
          java -jar "$CFR_JAR" "$SOURCE_JAR" --outputdir build/mc_src

          # 4. 验证反编译结果
          JAVA_COUNT=$(find build/mc_src -name "*.java" | wc -l)
          echo "📊 反编译完成，Java 文件数: $JAVA_COUNT"
          if [ "$JAVA_COUNT" -gt 0 ]; then
            echo "🎉 成功！反编译出纯 Java 源码。"
          else
            echo "⚠️ 未产生 Java 文件，可能反编译失败。"
          fi

      # 执行模组构建
      - name: 执行模组构建
        run: ./gradlew build --no-daemon

      # ========== 上传产物 1：模组 Jar ==========
      - name: 上传模组构建产物
        uses: actions/upload-artifact@v4
        with:
          name: UltimateScaler-${{ github.sha }}
          path: build/libs/*.jar
          if-no-files-found: error

      # ========== 上传产物 2：反编译后的 Java 源码（如果成功） ==========
      - name: 上传 Minecraft 源码（反编译后，纯 Java）
        uses: actions/upload-artifact@v4
        with:
          name: minecraft-sources-${{ env.MC_VERSION }}
          path: build/mc_src
          if-no-files-found: ignore

      # ========== 上传产物 3：所有缓存 jar（备用） ==========
      - name: 上传 Minecraft 缓存 JAR 包（备用）
        uses: actions/upload-artifact@v4
        with:
          name: minecraft-cache-all-jars
          path: build/all_jars/*.jar
          if-no-files-found: ignore
